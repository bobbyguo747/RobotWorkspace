scan_filter_chain:
- name: scan_filter
  type: laser_filters/LaserScanSectorFilter
  params:
    angle_min: 2.100                     # if not specified defaults to 0.0
    angle_max: 4.189                   # if not specified defaults to 0.0
    range_min: 0.01                      # if not specified defaults to 0.0
    range_max: 10.0                      # if not specified defaults to 100000.0
    clear_inside: true                  # if not specified defaults to true
    invert: false                       # (!clear_inside) if not specified defaults to false
# - name: scan_filter_1
  # type: laser_filters/LaserScanSectorFilter
  # params:
    # angle_min: 3.488                     # if not specified defaults to 0.0
    # angle_max: 4.012                    # if not specified defaults to 0.0
    # range_min: 0.01                      # if not specified defaults to 0.0
    # range_max: 10.0                      # if not specified defaults to 100000.0
    # clear_inside: true                  # if not specified defaults to true
    # invert: false                       # (!clear_inside) if not specified defaults to false
# - name: shadows
  # type: laser_filters/ScanShadowsFilter
  # params:
    # min_angle: 3
    # max_angle: 200
    # neighbors: 6
    # window: 4